% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_hier_linear.R
\name{sim_hier_linear}
\alias{sim_hier_linear}
\title{Simulate Hierarchical Linear Model Data}
\usage{
sim_hier_linear(
  nreg = 100,
  nobs = 10,
  nvar = 2,
  nz = 3,
  const = TRUE,
  het_observed = c("none", "linear", "step", "friedman"),
  target_var_betabar = 1,
  target_var_eps = 0.5,
  sigma_sq = 1,
  seed = NULL
)
}
\arguments{
\item{nreg}{Number of cross-sectional units.}

\item{nobs}{Number of observations per unit.}

\item{nvar}{Number of X variables (excluding intercept if const=TRUE).}

\item{nz}{Number of Z variables. Set to 0 for no observed heterogeneity.}

\item{const}{Logical. Include an intercept in X? Default TRUE.}

\item{het_observed}{Character. Functional form of observed heterogeneity.
Options: "none", "linear", "step", "friedman".}

\item{target_var_betabar}{Numeric. Target variance for the first coefficient
of the observed component betabar_i = f(Z_i). Default 1.0.}

\item{target_var_eps}{Numeric. Target variance for each coefficient in the
unobserved component eps_i. Default 0.5.}

\item{sigma_sq}{Numeric. Error variance for y_i = X_i * beta_i + e_i. Default 1.0.}

\item{seed}{Integer. Optional random seed.}
}
\value{
A list containing:
\item{regdata}{List of length nreg. Each element is a list with y, X, XpX, Xpy.}
\item{Z}{nreg x nz matrix of covariates (NULL if nz=0).}
\item{true_values}{List with beta_true, betabar_true, eps_true, sigma_sq, Delta (if linear).}
}
\description{
Generates simulated data from a hierarchical linear model with optional
observed heterogeneity (linear or nonlinear via Z covariates).
}
