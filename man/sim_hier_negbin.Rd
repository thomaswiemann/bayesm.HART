% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_hier_negbin.R
\name{sim_hier_negbin}
\alias{sim_hier_negbin}
\title{Simulate Hierarchical Negative Binomial Data}
\usage{
sim_hier_negbin(
  nreg = 100,
  nobs = 50,
  nvar = 2,
  nz = 3,
  const = TRUE,
  het_observed = c("none", "linear", "step"),
  target_var_betabar = 0.5,
  target_var_eps = 0.25,
  alpha = 5,
  seed = NULL
)
}
\arguments{
\item{nreg}{Number of cross-sectional units.}

\item{nobs}{Number of observations per unit.}

\item{nvar}{Number of X variables (excluding intercept if const=TRUE).}

\item{nz}{Number of Z variables. Set to 0 for no observed heterogeneity.}

\item{const}{Logical. Include an intercept in X? Default TRUE.}

\item{het_observed}{Character. Functional form of observed heterogeneity.
Options: "none", "linear", "step".}

\item{target_var_betabar}{Numeric. Target variance for the first coefficient
of the observed component. Default 0.5.}

\item{target_var_eps}{Numeric. Target variance for each coefficient in the
unobserved component eps_i. Default 0.25.}

\item{alpha}{Numeric. Overdispersion parameter for negative binomial. Default 5.}

\item{seed}{Integer. Optional random seed.}
}
\value{
A list containing:
\item{regdata}{List of length nreg. Each element has y, X.}
\item{hessdata}{List of length nreg. Each element has hess (approx Hessian).}
\item{Z}{nreg x nz matrix of covariates (NULL if nz=0).}
\item{true_values}{List with beta_true, betabar_true, alpha, etc.}
}
\description{
Generates simulated data from a hierarchical negative binomial model with
optional observed heterogeneity via Z covariates.
}
